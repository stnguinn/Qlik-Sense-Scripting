#This script will first use the match function to find the index of the matching value, and then the pick function will select the corresponding value from the list '1', '3', '5'. 
The legend should now display the numbers in the correct order.

=pick(match(VW_CMS_RATING_RECORD,'1','3','5'), '1', '3', '5')
